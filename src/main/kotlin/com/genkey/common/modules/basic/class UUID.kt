package com.genkey.common.modules.basic

/*
ID used in the dbase for identifiers.
Currently, not defined what it is.
 */
class UUID
{
    companion object {
        val UNASSIGNED = UUID() //-1 or whatever
    }
}